// Update this if the repo moves or is renamed — every "report an issue" /
// "view source" link in the app reads from here instead of hardcoding a URL.
export const REPO_URL = 'https://github.com/byllzz/commandeer'
export const NEW_ISSUE_URL = `${REPO_URL}/issues/new`
